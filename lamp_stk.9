---

- hosts: all
  become: true
  tasks:

  - name: apache2, php 
    package:
      name:
        - "{{ apachepack }}"
        - "{{ phpack }}"
        - "{{ sql }}"
      state: latest
      update_cache: yes

    name: mysql
    apt: 
      name:  mysql-server
